<?php

/*
|--------------------------------------------------------------------------
| Web Routes
|--------------------------------------------------------------------------
|
| Here is where you can register web routes for your application. These
| routes are loaded by the RouteServiceProvider within a group which
| contains the "web" middleware group. Now create something great!
|
 */

//Route::get('/', function () {
//    // return view('welcome');
//});

//Route::get('login', ['as' => 'login', 'uses' => 'Backend\LoginController@login']);
//Route::get('register', 'Backend\LoginController@register');
//Route::post('admin/login', 'Backend\LoginController@postLogin')->name('admin/login');
//Route::group(['middleware' => ['auth']], function () {
//    Route::get('main', 'Backend\MainController@main');
//});
